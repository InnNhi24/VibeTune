{
  "name": "vibetune-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend && npm run build --workspace=backend"
  },
  "dependencies": {
    "serverless-http": "^3.2.0"
  }
}

