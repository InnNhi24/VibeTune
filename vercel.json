{
  "builds": [
    {
      "src": "api/chat.ts",
      "use": "@vercel/node",
      "config": { "includeFiles": ["backend/dist/**", "backend/node_modules/**"] }
    },
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/chat"
    },
    {
      "src": "/(.*)",
      "dest": "/frontend/dist/$1"
    }
  ]
}

